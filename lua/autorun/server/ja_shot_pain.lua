hook.Add("EntityTakeDamage", "JaVOX Shot Pain Module", function(target, dmg)
    -- TODO: check both entities, if recipient is player and they have preset, call their javox 'self.inflictedDamage.shot.rarm, larm, etc.', or specific ones
    -- TODO: always have generic editions.
end)
